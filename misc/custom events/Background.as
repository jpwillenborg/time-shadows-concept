package  {
	
	import starling.display.Image;
	import starling.display.Sprite;
	import starling.display.Stage;
	//import starling.events.*;
	
	
	public class Background {
		
		static var floorSprite:Sprite;
		static var tileWidth:int = 48;
		static var tileHeight:int = 48;
		
		
		static function setup(stage:Stage) {			
			
			// NEW EVENT DISPATCHING
			
			/*blueCircle.addEventListener("check", updateX);
			function updateX(evt:Event, id:String):void {
				trace(id);
			}
			
			blueCircle.dispatchEventWith("check", true, "new param"); // used for checks instead of regular conditionals.*/
			
		}
	}
}
